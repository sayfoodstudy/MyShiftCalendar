# 3교대 달력알람 업로드/설치 안내

## 1. GitHub 저장소 만들기

1. https://github.com 접속
2. 로그인
3. 오른쪽 위 `+` → `New repository`
4. 저장소 이름 예: `ShiftCalendarAlarm`
5. Public 또는 Private 선택
6. `Create repository`

## 2. 파일 업로드

압축을 풀면 `ShiftCalendarAlarm` 폴더가 있습니다.

중요: `ShiftCalendarAlarm` 폴더 자체를 올리는 것이 아니라, 그 안의 내용을 저장소 최상단에 올려야 합니다.

저장소 첫 화면에 아래처럼 보여야 정상입니다.

```text
.github
app
build.gradle
gradle.properties
settings.gradle
README.md
업로드_설치_안내.md
```

만약 첫 화면에 `ShiftCalendarAlarm` 폴더 하나만 보이면 잘못 올라간 것입니다.

## 3. `.github` 폴더가 업로드 안 될 때

`.github`는 숨김 폴더라 빠질 수 있습니다.
그럴 때는 GitHub에서 직접 파일을 만드세요.

1. `Add file`
2. `Create new file`
3. 파일 이름에 아래를 그대로 입력

```text
.github/workflows/build-apk.yml
```

4. 압축 폴더 안의 `.github/workflows/build-apk.yml` 내용을 복사해서 붙여넣기
5. `Commit changes`

## 4. APK 빌드

1. 저장소 상단 `Actions` 클릭
2. `Build Android APK` 클릭
3. 필요하면 `Run workflow` 클릭
4. 초록색 체크가 나올 때까지 기다림
5. 아래 `Artifacts`에서 다운로드

```text
shift-calendar-alarm-debug-apk
```

6. 압축을 풀면 아래 APK가 있습니다.

```text
app-debug.apk
```

## 5. 삼성폰 설치

1. `app-debug.apk`를 삼성폰으로 옮김
2. APK 파일을 누름
3. 알 수 없는 앱 설치 허용
4. 설치
5. 앱 실행
6. 첫 실행에서 주간 시작일 설정

## 6. 첫 테스트

1. 주간 시작일 설정
2. 달력에 주간/당직/비번 표시 확인
3. 날짜 눌러 근무 변경 테스트
4. 설정 → 근무 종류 관리에서 `연차` 같은 항목 추가
5. 기간 일정 추가 후 색 라인이 표시되는지 확인
